# 🚀 كيفية استخدام نسخة Offline

## ⚠️ ملاحظة مهمة لمتصفح Brave

**Brave يدعم IndexedDB بنسبة 100%** ✅

لكن فيه إعداد لازم تفعله:

### تفعيل الدعم في Brave:

1. **افتح Brave**
2. **اذهب لـ:** `brave://settings/privacy`
3. **في قسم "Cookies and other site data"**
4. **اختر:** "Allow all cookies" أو "Block third-party cookies in Incognito" (المهم ما يكونش "Block all cookies")
5. **تأكد إن "Clear cookies and site data when you close all windows" معطّل**

---

## 📋 خطوات التشغيل الصحيحة

### 1️⃣ **المرة الأولى (بتكون Online)**
```bash
# ادخل على المجلد
D:\OL\offline-version\

# افتح index.html في Brave
# أو اعمل Serve بأي طريقة:
# - Live Server في VS Code
# - Python: python -m http.server 8000
# - Node: npx serve
```

**لازم:**
- ✅ يكون فيه إنترنت في أول مرة
- ✅ تسجل دخول بنجاح
- ✅ البيانات تحمل من Firebase

### 2️⃣ **اختبار Offline**

**في Brave:**
1. افتح Developer Tools (F12)
2. اذهب لـ Network tab
3. غيّر "Online" لـ "Offline"
4. أو اقطع WiFi

**جرب:**
- ✅ أضف إيصال جديد
- ✅ ابحث في البيانات
- ✅ اطبع إيصال

### 3️⃣ **اختبار المزامنة**

1. رجّع الاتصال (Online)
2. اضغط زر "مزامنة" في الهيدر
3. أو انتظر 5 ثواني (مزامنة تلقائية)
4. ✅ البيانات هتروح لـ Firebase

---

## 🔧 حل المشاكل الشائعة

### ❌ "يرجعني لصفحة الدخول"

**السبب:** ملف `script.js` ماكانش بيحمل

**الحل:**
```html
<!-- في index.html، تأكد إن الترتيب صح: -->
<script src="script.js?v=24"></script>
<script src="offline-script.js?v=24"></script>
```

**التأكد:**
1. افتح Console (F12 → Console)
2. لازم تشوف: "✅ Firebase initialized"
3. لازم تشوف: "✅ IndexedDB initialized"
4. لازم تشوف: "✅ Offline mode initialized"

### ❌ "مفيش بيانات تظهر"

**السبب:** IndexedDB فاضية

**الحل:**
1. تأكد إنك فاتح بالإنترنت أول مرة
2. سجل دخول
3. استنى تحميل البيانات
4. بعدها جرب Offline

### ❌ "البيانات متحفظش"

**السبب:** إعدادات Brave بتمنع التخزين

**الحل:**
1. `brave://settings/content/cookies`
2. اضف الموقع للـ Exceptions: `Allow`
3. أو عطّل "Block third-party cookies"

### ❌ "مش قادر أضيف إيصال"

**الحل:**
```javascript
// افتح Console واكتب:
await initOfflineDB();
console.log('DB:', offlineDB);
// لازم يظهر: DB: IDBDatabase {...}
```

لو ظهر `null` يبقى فيه مشكلة في IndexedDB.

---

## ✅ التأكد إن كل حاجة شغالة

افتح Console (F12) وشوف:

```
✅ IndexedDB initialized
✅ Firebase initialized (or error - that's OK)
✅ Offline mode initialized
✅ Firebase functions patched for offline support
```

لازم تظهر الرسايل دي!

---

## 🧪 اختبار سريع

### اختبار 1: Online
1. افتح بالإنترنت
2. سجل دخول
3. أضف إيصال
4. ✅ يحفظ في Firebase

### اختبار 2: Offline
1. اقطع الإنترنت
2. أضف إيصال جديد
3. ✅ يظهر رسالة: "تم الحفظ محلياً"
4. ✅ يظهر في الجدول

### اختبار 3: المزامنة
1. رجّع الإنترنت
2. اضغط "مزامنة"
3. ✅ البيانات تروح لـ Firebase
4. ✅ تختفي من "قائمة الانتظار"

---

## 📱 تثبيت على Brave (PWA)

1. افتح التطبيق في Brave
2. اضغط على الأيقونة في شريط العنوان (📱)
3. اختر "Install Dent Treasury"
4. هيفتح كتطبيق منفصل!

---

## 🆘 لو فيه مشكلة

ارفع لي:
1. صورة من Console (F12) - كل الرسايل
2. Brave version (brave://version/)
3. هل فتحت بالـ File Protocol ولا Server?

**ملاحظة:** التطبيق ممكن ما يشتغلش صح لو فتحته بالـ File Protocol (`file:///`) في بعض المتصفحات. استخدم Local Server:

```bash
# أسهل طريقة (لو عندك Python):
python -m http.server 8000

# أو (لو عندك Node):
npx serve

# أو في VS Code:
# Install "Live Server" extension → Click "Go Live"
```

وبعدين افتح: `http://localhost:8000`

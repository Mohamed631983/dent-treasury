# نسخة Offline - مدفوعات الصناديق والمعاشات

## ✅ التعديلات الجديدة:
### 1. نظام تسجيل دخول Offline
- ✅ تقدر تدخل من غير نت (من أول مرة)
- ✅ تقدر تضيف users من غير نت
- ✅ مزامنة تلقائية لما النت يرجع

### 2. الملفات الجديدة:
- `offline-auth.js` ← نظام Login الجديد
- `offline-sync.js` ← مزامنة البيانات
- `sw.js` ← Service Worker
- `manifest.json` ← PWA

## الميزات الجديدة

### 1. مؤشر الحالة (Status Indicator)
في الهيدر هتلاقي:
- 🟢 **متزامن** - كل حاجة تمام
- 🔵 **جاري المزامنة** - بينزل بيانات على Firebase
- 🟠 **Offline** - النت فاصل، شغال Local فقط
- 🔴 **X عملية معلقة** - فيه بيانات محتاجة تترفع

### 2. زر المزامنة اليدوي
- في الهيدر زر "مزامنة"
- اضغط عليه لما تكون Online عشان ترفع البيانات المعلقة

### 3. المزامنة التلقائية
- كل 30 ثانية بيحاول يرفع البيانات المعلقة
- لما النت يرجع، بيحاول يزامن على طول

## 📦 خطوات النقل:

### على الجهاز الجديد:

#### 1. **انسخ الملفات دي** (كلهم):
```
offline-version/
├── index.html          ← معدل (شامل كل التعديلات)
├── styles.css          ← نفسه
├── script.js           ← معدل (API Key الجديد)
├── offline-auth.js     ← جديد (نظام Login)
├── offline-sync.js     ← جديد (المزامنة)
├── sw.js              ← جديد (Service Worker)
├── manifest.json      ← جديد (PWA)
└── README.txt         ← الشرح

#### 4. **اشتغل Offline** عادي!

## 🔄 المزامنة:

### لما النت يرجع:
1. اضغط على **زر "مزامنة"** في الهيدر
2. أو استنى 30 ثانية (مزامنة تلقائية)
3. كل البيانات هترفع لـ Firebase

### ملاحظة مهمة:
- البيانات بتتخزن على الجهاز **أولاً**
- لما يكون فيه نت، بتتزامن مع Firebase
- لو فتحت من جهاز تاني، هتشوف نفس البيانات

## 👥 إدارة المستخدمين:

### إضافة مستخدم Offline:
1. روح **"إدارة المستخدمين"**
2. املأ البيانات
3. اضغط **"إضافة"**
4. المستخدم هيتخزن محلي
5. لما النت يرجع، هيتزامن تلقائي

### ملاحظة:
- كل جهاز ليه users مستقلين في الأول
- لما النت يتوصل، كل الأجهزة هتشوف نفس الـ users

## ⚠️ تنبيهات:

### الأمان:
- الباسوردات **مشفرة** في الكود
- بس **الجهاز بتاعك** ممكن يتفتح
- لو حد وصل للجهاز، يقدر يشوف الـ users

### البيانات:
- لو مسحت **Browser Data** = هتضيع البيانات المحلية
- بس لو فيه نت، هترجع من Firebase
- لو مفيش نت ومسحت = خلاص ضاعت

## 🚀 المميزات الجديدة:

### شغال في كل الأحوال:
- ✅ Online (مع نت)
- ✅ Offline (من غير نت)
- ✅ تسجيل خروج تلقائي (30 دقيقة)
- ✅ مزامنة تلقائية
- ✅ PWA (ممكن تثبته كـ App)

### عيوب بسيطة:
- ⚠️ أول مرة لازم تفتح الملفات من الجهاز (مش رابط)
- ⚠️ البيانات على كل جهاز مستقلة لحد ما يتزامن


## 📞 دعم فني:

لو حاجة مش شغالة:
1. افتح **Console** (F12)
2. شوف الأخطاء
3. تأكد إن كل الملفات موجودة
4. أعمل **Refresh** (Ctrl+F5)

## 🎯 ملخص سريع:

**الفرق عن النسخة الأصلية:**
- قبل: لازم نت أول مرة
- دلوقتي: مش لازم نت خالص

**الخطوة الوحيدة:**
انسخ الملفات 7 دول للجهاز وافتح `index.html` واشتغل!

**Good Luck!** 🎉

